export const metadata = {
  title: "Payload Admin - SCISS",
  description: "Payload CMS Admin Interface",
};

export default function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
